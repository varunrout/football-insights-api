# app/util/constants.py

# mplsoccer uses StatsBomb pitch as 120x80
PITCH_LENGTH = 120
PITCH_WIDTH = 80

# Zone grid settings for xT/zone control models
XT_GRID_ROWS = 12
XT_GRID_COLS = 16

# Define key zones (optional later)
ZONE_14 = ((40, 80), (60, 120))  # center top of box
WIDE_LEFT = ((0, 20), (0, 40))
